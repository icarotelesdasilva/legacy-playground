cmake_minimum_required(VERSION 3.16)

# Carrega as funções internas do ESP-IDF primeiro
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

# Nome do projeto (pode ser o mesmo do seu repo)
project(firmware)
